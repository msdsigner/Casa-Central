# KEEP SECRETS SAFE (Do not delete these!)
credentials.json
token.pickle
token.json
*.pickle
service_account*.json
.env
.env.*
*.env

# Keep OS junk out
.DS_Store
Thumbs.db

# (Images are no longer listed here, so they will now update perfectly!)